# AIエージェントに高品質なLPを作らせる完全ガイド

## 問題の本質分析

AIエージェントに「良いデザインのLPを作って」と指示しても、実用レベルのものができない根本原因：

1. **抽象的な指示の限界**: 「モダンで」「かっこいい」は定義が曖昧
2. **参照画像の限界**: スクリーンショットからは微細なスペーシング、アニメーション、レスポンシブの実装が伝わらない
3. **イテレーションの罠**: 40点の構造を改善しても60点にしかならない（根本設計に問題がある）
4. **デザインシステムの欠如**: 一貫性のないスタイルがバラバラに適用される

---

## 解決策: 5つの戦略的アプローチ

### 戦略1: 「コードベース注入法」（最も効果的）

**核心原理**: AIに「良いデザインを作れ」ではなく、「この高品質なコードを参考にカスタマイズしろ」と指示する

#### 具体的手順

1. **高品質なオープンソーステンプレートを入手**
   - leoMirandaa/shadcn-landing-page (1852 stars)
   - nobruf/shadcn-landing-page (1176 stars)
   - danmindru/page-ui (1614 stars)
   - ixartz/SaaS-Boilerplate (6775 stars)

2. **コンポーネントのソースコードをAIに直接渡す**
   ```
   プロンプト例:
   「以下のHeroコンポーネントのコードをベースに、[あなたのサービス]用にカスタマイズしてください。
   構造、Tailwindクラス、アニメーションの品質を維持しつつ、コンテンツのみ変更してください。」
   
   [ここにHero.tsxのコードを貼り付け]
   ```

3. **重要ポイント**:
   - コードをそのまま渡すことで、AIは「創造」ではなく「編集」モードになる
   - 品質の高いベースがあれば、それを壊すことは少ない

---

### 戦略2: 「デザインシステム先行法」

**核心原理**: 抽象的な「良いデザイン」ではなく、数値化された具体的なルールを先に定義する

#### ステップ1: デザイントークンを定義

```css
/* このCSSをまずAIに渡す */
:root {
  /* カラーシステム */
  --primary-hue: 250;
  --primary: hsl(var(--primary-hue) 91% 58%);
  --primary-foreground: hsl(0 0% 100%);
  
  /* タイポグラフィスケール */
  --text-xs: 0.75rem;
  --text-sm: 0.875rem;
  --text-base: 1rem;
  --text-lg: 1.125rem;
  --text-xl: 1.25rem;
  --text-2xl: 1.5rem;
  --text-3xl: 1.875rem;
  --text-4xl: 2.25rem;
  --text-5xl: 3rem;
  --text-6xl: 3.75rem;
  
  /* スペーシングスケール（8pxベース）*/
  --space-1: 0.25rem;   /* 4px */
  --space-2: 0.5rem;    /* 8px */
  --space-3: 0.75rem;   /* 12px */
  --space-4: 1rem;      /* 16px */
  --space-6: 1.5rem;    /* 24px */
  --space-8: 2rem;      /* 32px */
  --space-12: 3rem;     /* 48px */
  --space-16: 4rem;     /* 64px */
  --space-24: 6rem;     /* 96px */
  --space-32: 8rem;     /* 128px */
  
  /* シャドウシステム */
  --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
  --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1);
  --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1);
  --shadow-xl: 0 20px 25px -5px rgb(0 0 0 / 0.1);
  
  /* border-radius */
  --radius-sm: 0.25rem;
  --radius-md: 0.5rem;
  --radius-lg: 0.75rem;
  --radius-xl: 1rem;
  --radius-2xl: 1.5rem;
  --radius-full: 9999px;
}
```

#### ステップ2: コンポーネント設計ルールを定義

```
プロンプト例:
「以下のデザインシステムに厳密に従ってLPを作成してください：

1. コンテナ幅: max-width: 1280px, padding-x: 24px
2. セクション間: 96px〜128pxの余白
3. ヘッドライン: text-5xl/font-bold, line-height: 1.1
4. サブヘッド: text-xl/font-normal, text-muted-foreground
5. CTAボタン: px-8 py-4, rounded-xl, グラデーション背景
6. カード: rounded-2xl, shadow-lg, hover時にshadow-xl + translateY(-4px)
7. アニメーション: 全てease-out 300ms, ホバー時のみ変化」
```

---

### 戦略3: 「コンポーネント分離ビルド法」

**核心原理**: LP全体を一度に作らせず、コンポーネント単位で高品質に仕上げてから組み合わせる

#### 実行手順

1. **セクションリストを定義**
   - Header/Navigation
   - Hero Section
   - Social Proof (ロゴ、数字)
   - Features Section
   - How It Works
   - Testimonials
   - Pricing
   - FAQ
   - CTA Section
   - Footer

2. **各コンポーネントを個別に完成させる**
   ```
   プロンプト例（Heroのみ）:
   「Heroセクションのみを作成してください。
   
   要件:
   - 左右2カラムレイアウト（テキスト | ビジュアル）
   - メインヘッドライン: グラデーションテキスト使用
   - サブヘッドライン: 2行まで、muted color
   - CTA: プライマリボタン + セカンダリボタン
   - 右側: モックアップ画像用のプレースホルダー
   - 背景: subtle gradient + floating shapes
   
   品質要件:
   - ホバーアニメーション必須
   - レスポンシブ（モバイルでは縦並び）
   - ダークモード対応」
   ```

3. **品質チェック後に次のセクションへ**

4. **最後に統合**

---

### 戦略4: 「アニメーションライブラリ活用法」

**核心原理**: AIにアニメーションを「創造」させず、既存の高品質ライブラリを指定する

#### 推奨ライブラリ（コード取得可能）

1. **Magic UI** (https://magicui.design/)
   - shimmer-button
   - bento-grid
   - marquee
   - number-ticker
   - globe
   - particles

2. **Aceternity UI** (https://ui.aceternity.com/)
   - spotlight
   - 3d-card
   - animated-beam
   - background-gradient

3. **shadcn/ui + Framer Motion組み合わせ**

#### 使用方法

```
プロンプト例:
「以下のMagic UIのShimmerButtonコンポーネントをCTAボタンとして使用してください。

[ShimmerButtonのコードをここに貼り付け]

このボタンを使って、以下の内容のCTAセクションを作成してください：
- ヘッドライン: 「今すぐ始めよう」
- ボタンテキスト: 「無料で試す」」
```

---

### 戦略5: 「プロ品質テンプレート改造法」

**核心原理**: ゼロから作らせず、既に完成度の高いテンプレートのカスタマイズに特化させる

#### 手順

1. **高品質テンプレートをclone**
   ```bash
   git clone https://github.com/leoMirandaa/shadcn-landing-page.git
   ```

2. **コンテンツ置換のみをAIに指示**
   ```
   プロンプト例:
   「このLPテンプレートのコンテンツを以下のサービス用に置き換えてください。
   デザイン、レイアウト、アニメーションは一切変更しないでください。
   
   サービス名: [名前]
   サービス説明: [説明]
   ターゲット: [ターゲット]
   主要機能: [機能1], [機能2], [機能3]
   価格プラン: [プラン情報]」
   ```

3. **段階的にカスタマイズを追加**
   - まずコンテンツ置換のみ
   - 次にカラースキーム変更
   - 最後に細部調整

---

## 実践的なプロンプトテンプレート

### 基本構造

```
# 役割設定
あなたは10年以上の経験を持つシニアフロントエンドエンジニア兼UIデザイナーです。
Stripe、Linear、Vercel、Raycast等の高品質なSaaSプロダクトのLPを参考にしています。

# 技術スタック
- HTML/CSS（Tailwind CSS v3.4）
- アニメーション: CSS transitions/transforms
- レスポンシブ: モバイルファースト

# デザイン原則（必ず守ること）
1. 余白は多めに取る（窮屈に見せない）
2. コントラスト比は4.5:1以上を確保
3. ホバーステートは全てのインタラクティブ要素に適用
4. アニメーションは300ms以下、ease-outを基本とする
5. フォントサイズは最低16px（本文）

# 色彩設計
- プライマリ: [指定色]
- 背景: 純白ではなく微かな色味（#fafafa推奨）
- テキスト: 真っ黒ではなく深いグレー（#1a1a1a推奨）

# 必須要素
- グラデーションを使ったアクセント
- 微細なシャドウ（drop-shadow-lg等）
- ホバー時のtransformアニメーション
- 角丸は大きめ（rounded-xl以上推奨）

# 作成するセクション
[セクションリスト]

# 禁止事項
- Lorem ipsumは使用しない
- 画像のaltは必ず記述
- インラインスタイルは使用しない
```

---

## 最終推奨フロー

```
┌─────────────────────────────────────────────────────┐
│  Phase 1: 準備（人間が行う）                         │
│  ├── 参考LPを3つ選定                                │
│  ├── そのうち1つからコードを取得（DevTools or GitHub）│
│  └── デザインシステムを数値で定義                    │
└──────────────────────────┬──────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────┐
│  Phase 2: コンポーネント単位ビルド（AIに指示）       │
│  ├── Heroセクション → レビュー → 承認               │
│  ├── Featuresセクション → レビュー → 承認           │
│  ├── ... (各セクション)                              │
│  └── 全セクション完成                                │
└──────────────────────────┬──────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────┐
│  Phase 3: 統合と微調整（AIに指示）                   │
│  ├── 全セクションを1ファイルに統合                   │
│  ├── セクション間の余白調整                          │
│  ├── レスポンシブの最終確認                          │
│  └── アニメーションの一貫性確認                      │
└──────────────────────────┬──────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────┐
│  Phase 4: ポリッシュ（AIに指示）                     │
│  ├── マイクロインタラクション追加                    │
│  ├── ローディング状態の追加                          │
│  └── アクセシビリティチェック                        │
└─────────────────────────────────────────────────────┘
```

---

## コード取得リソース一覧

| リソース | URL | 特徴 |
|---------|-----|------|
| shadcn-landing-page | github.com/leoMirandaa/shadcn-landing-page | Reactベース、コピペ可能 |
| page-ui | github.com/danmindru/page-ui | コンポーネント豊富 |
| Magic UI | magicui.design | アニメーション特化 |
| Aceternity UI | ui.aceternity.com | エフェクト系豊富 |
| shadcn/ui | ui.shadcn.com | 基本コンポーネント |
| Tailwind UI | tailwindui.com (有料) | 最高品質 |

---

## まとめ

**成功の鍵は「AIに創造させない」こと**

1. 高品質なコードを渡して「編集」させる
2. 抽象的な指示ではなく「数値」で指定する
3. 一括ではなく「分割」して品質管理する
4. アニメーションは「既存ライブラリ」を使う
5. ゼロからではなく「テンプレート改造」で始める

AIは「無から有を生む」のが苦手だが、「有を加工する」のは得意。
この特性を活かすことが、プロレベルのLP作成への近道。
